#!/bin/bash
# Auto-reload emacs pywal theme when colors change

# Check if emacs daemon is running (check for "emacs" not "emacsd")
if pgrep -f "emacs --daemon" >/dev/null 2>&1; then
    emacsclient -e "(reload-pywal-theme)" 2>/dev/null &
fi
